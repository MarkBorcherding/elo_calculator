%h3 Add Club

= form_for @club do |f|
  .row
    .small-12.large-6.columns
      = f.label :name do
        Name
        = f.text_field :name, placeholder: 'Club Name'
  %p
    = f.submit 'Create', class: 'button'
